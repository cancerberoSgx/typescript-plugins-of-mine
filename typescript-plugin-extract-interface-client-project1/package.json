{
  "name": "typescript-plugin-extract-interface-client-project1",
  "version": "1.0.8",
  "private": true,
  "description": "for testing this plugin as client project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"No tests here\" && exit 0",
    "build": "echo \"no build here\" && exit 0",
    "doc": "echo nothing && exit 0",
    "clean": "rm -rf node_modules/ package-lock.json",
    "install-published-plugins-dependencies": "rm -rf node_modules/ package-lock.json; cp package.json package-linked.json; cp package-unlinked.json package.json; npm i",
    "install-linked-plugins-dependencies": "rm -rf node_modules/ package-lock.json; cp package-linked.json package.json; npm i"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^10.12.0"
  },
  "devDependencies": {
    "typescript": "^3.1.3",
    "typescript-plugin-proactive-code-fixes": "file:../typescript-plugin-proactive-code-fixes"
  }
}
