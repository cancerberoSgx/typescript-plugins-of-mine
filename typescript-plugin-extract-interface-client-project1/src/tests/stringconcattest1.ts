const name = 'Sebastian'

const where = 'here'

const msg1 = "Hello " + name + " we are \"glad\" you have " + (1 + 2 + 3) + " years old"

const msg2 = `Hello ${name}. We are glad you are ${where}`


// &%&% reorderParams("prettyFunction", [1, 0])
function prettyFunction(a: string, b: number, c: Date[]): boolean {
  return null
}

class cccc {
  method(g: string): string {
    return prettyFunction(g, 2, [new Date()]) + ''
  }
}

